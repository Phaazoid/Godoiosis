# 7 — Limb slots + the effective-stat spine + MOV

**Size L · gameplay code (user types) · AFTER 6 · the keystone session — jobs (9), parts (10), and transmutation (11) all plug into what this builds.** Source: [will-and-death.md](../design/will-and-death.md) → "The limb-slot model" + [jobs.md](../design/jobs.md) → "MOV" + [stats.md](../design/stats.md) effective-stat layer. This is the remaining build half of #33 (closed; design canon).

```
Project: Iosis (tactical RPG, Godot 4.6, GDScript). Work in C:\Iosis\Godoiosis. Read CLAUDE.md first (collaboration contract: user hand-types ALL gameplay code — complete typed code blocks + file anchors + the why, verify by reading the real file after; Laws #1/#2; sharp edges). Then read IN FULL, as canon: docs/design/will-and-death.md (the limb-slot model section especially), docs/design/jobs.md (the MOV section), docs/design/stats.md (band doctrine + effective stats). Skim docs/design/alchemy-kit.md "Aura data model" for the limb tax. Code to read: Classes/units/UnitInstance.gd (maimed_part, spend_will_for_down, _refresh_derived_stats — the intended seam), Classes/units/Unit.gd (lifecycle), Classes/units/MovementComponent.gd (where move range comes from TODAY — you are retiring that source), Classes/actions/PlanResolver.gd (Lethality.MAIMED preview), Classes/actions/RescueAction.gd, Classes/weapons/WeaponData.gd, Classes/flow/ScenarioUnitEntry.gd (check whether maim/Will state is scenario-persisted — if yes, field changes are a data migration needing user sign-off).

Goal: limbs become equipment slots, effective stats derive from them, and MOV finally has its formula. Numbers are placeholders -> named constants, "# playtest-tunable". Terse comments.

1. THE SLOT MODEL on UnitInstance (persistent side of the #8 seam). Four slots — enum LimbSlot {ARM_L, ARM_R, LEG_L, LEG_R} — each holding: NATURAL (stat = the unit's innate STR for arms / innate DEX for legs), EMPTY (0, maimed), or a PROSTHETIC (its own built-in stat; content lands in prompt 10 — model the entry now, author nothing). Replace the single maimed_part enum with this model; derive is_maimed() from any non-natural-non-prosthetic slot. maimed_part is currently NOT @export-persisted, so no .tres migration — but confirm ScenarioUnitEntry doesn't save it before deleting.

2. EFFECTIVE STATS. Effective STR = mean of the two ARM slots, ROUNDED UP; effective DEX = mean of the two LEG slots, rounded up (one rule, four slots: arms carry strength, legs carry quickness). Route get_effective_stat(stat) as: base -> limb-slot substitution (STR/DEX only) -> stat_modifiers/gear -> (job ceiling clamp arrives in prompt 9 — leave a marked seam). Reroute prompt 6's bands through EFFECTIVE stats (a maimed leg lowers effective DEX, which lowers MOV — that thread is the point). Fill the _refresh_derived_stats() placeholder or delete it in favor of computed getters — pick one truth, no caching bugs.

3. MOV = job base + DEX band (closes audit A4). const JOBLESS_MOV_BASE := 4 (prompt 9 swaps in the main job's base); get_mov() = job_base + Stats.dex_mov_band(effective DEX). Retire whatever MovementComponent uses today; migrate every reader. MOV is a READOUT — never authored per-unit, never slot-averaged itself. Weight x MOV coarse-threshold penalty: land the constant + threshold function beside get_weight() from prompt 6, applied at get_mov() — coarse steps ONLY (a heavy-load step, not per-point). THEN the LEG-STATE THROTTLE (dev ruling 2026-07-14, jobs.md MOV + will-and-death.md limb model), applied LAST: exactly one EMPTY leg slot -> halve final MOV, rounded UP (one leg always beats none); both EMPTY -> MOV = 1 flat, hard override of everything. Prosthetic-filled slots count as functional legs (the throttle keys on empty slots, not maim history). Test both rungs.

4. MAIM = the next slot by FIXED ROTATION: weapon arm -> off leg -> off arm -> weapon leg, natural limbs first, prosthetics targeted only when no natural limb remains; a maimed prosthetic DETACHES TO INVENTORY as recoverable gear (the investment survives). Multi-maim is allowed and NEVER escalates to death — a fully-maimed unit still just downs ("Will never kills" is absolute). Rewrite spend_will_for_down()'s maim branch onto the rotation. Show "next at risk" on the unit panel (the rotation is deterministic — Law #1 — so the player may see it).

5. VERB LOCKS (provisional set, canon): ANY missing arm -> two-handed/heavy patterns locked + rescue-carry locked. WeaponData needs a two_handed flag (additive @export with a false default — no migration needed; mark existing heavy weapons with the user). Enforce at BOTH the action menu and SquadManager.queue_action, mirroring how the main-action rule is enforced. Legs carry no verb lock — the DEX averaging IS the effect.

6. AURA LIMB TAX (audit A3): on maim, -1 aura point off the unit's HIGHEST pool; ties break deterministically (doc says ties -> primary affinity — the affinity field arrives in prompt 11, so break ties by element enum order for now with a TODO(11) comment). Restored by regrowth — which is between-battle territory, NOT built, so the tax is one-way for now; leave the seam comment.

7. LAW #2. The MAIMED lethality preview already threads through PlanResolver — verify it still holds against the new can-afford path, and that the preview's which-limb matches the rotation's actual pick. The text-popup preview form is a stretch goal; the icon must not lie TODAY.

Do NOT touch: prosthetic CONTENT + integrated weapons (prompt 10), Crisis preview (prompt 8), job ceilings (prompt 9), regrowth/recovery (parked — grill-queue.md). If the doc is silent on something, STOP and ask; do not invent.

Done when: a maimed 7-STR unit reads effective STR 4 (ceil(7+0)/2); a leg maim visibly drops MOV through the DEX band AND the leg throttle (one empty leg halves final MOV rounded up; two empty pins MOV at 1); the rotation picks limbs in order across multi-maims with prosthetics last + recoverable; one-armed units cannot queue two-handed attacks or rescues (menu AND queue_action both refuse); the maim preview icon matches execution; tests cover slot averaging, rotation, verb locks, and the MOV formula; suite green; CLAUDE.md architecture map updated (the #33 "left" paragraph is now largely built — rewrite it); committed.
```
