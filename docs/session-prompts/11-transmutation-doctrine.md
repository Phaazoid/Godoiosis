# 11 — Transmutation doctrine catch-up (the #30 lane)

**Size L · gameplay code (user types) · after 7 (refines its aura-tax tiebreak); may run BEFORE 9/10 — it advances [#30](https://github.com/Phaazoid/Godoiosis/issues/30), the open P1 issue.** Source: [transmutation-model-proposal.md](../design/transmutation-model-proposal.md) → *Grill resolutions* (2026-07-04) + [alchemy-kit.md](../design/alchemy-kit.md) → *Aura data model* (audit A3) + [will-and-death.md](../design/will-and-death.md) → strain + [resolution-pipeline.md](../design/resolution-pipeline.md) → A7 fizzles. The #30 substrate is BUILT and green; this session brings the code up to the doctrine the docs ratified after it landed.

```
Project: Iosis (tactical RPG, Godot 4.6, GDScript). Work in C:\Iosis\Godoiosis. Read CLAUDE.md first (collaboration contract: user hand-types ALL gameplay code — complete typed code blocks + anchors + why; Laws #1/#2). Then read IN FULL as canon: docs/design/transmutation-model-proposal.md (the Grill resolutions section is the spec), docs/design/alchemy-kit.md (the Aura data model section is CANONICAL for aura), docs/design/will-and-death.md "Transmutation strain" (strain is a COST, never damage), docs/design/resolution-pipeline.md (A7: affordability is a resolver-stage check vs threaded HP; unaffordable-mid-pass = previewed fizzle, NOT a BREAK). Code to read: Classes/items/ (RuneData, RuneCatalog, TransmutationData, TransmutationCatalog, Flourish, EquippableData — the #30 substrate), Classes/units/UnitInstance.gd (the aura map + prompt 7's limb-tax TODO), Classes/actions/PlanResolver.gd + ResolvedOutcome.gd (skipped-with-reason precedent: counter liveness), the firing path (grep for where transmutations resolve), tests for the above.

Goal: close the gap between the built #30 substrate and the 2026-07-04/05 doctrine. Code follows docs — where the substrate disagrees with the doc, the substrate changes. Numbers are placeholders -> named constants, "# playtest-tunable". Terse comments.

1. AFFINITY SET (audit A3). Add the genetic, immutable, per-element binary affinity set to UnitInstance as its OWN persisted field — NOT derivable from aura >= 1, because the limb tax can zero a pool while the growth right persists. Aura points grow ONLY within affinity. Rebecca rule: empty affinity set = nothing, no brute force, ever. Isaac/Alkahest: the hidden sixth is NEVER displayed — represent as all-five affinity + a hidden flag; no Alkahest bar anywhere in UI. Seed UnitData/dev-editor support (affinity checkboxes; aura spinners clamp to affinity).

2. TWO-KNOB RUNE SIZES: cap 1/2/3 and capacity 1/3/6 on RuneData (constants). Validate transcription/flourish content against both knobs at authoring AND at load (the catalogs should refuse malformed .tres loudly). Migrate existing rune .tres to carry the knobs (additive fields with sane defaults — show the user the diffs).

3. TEMPER + TRAINED LEEWAY + STRAIN. Channeling gates on aura floors (floors = sigil weight; temper is EARNED, never brute-forced). Within the trained leeway band, casting BELOW the floor is legal but costs strain — recoil HP. Strain is a COST, not damage: it NEVER goes through take_damage, never downs, never maims, never triggers Crisis, and is AFFORDABILITY-GATED — if it would leave the caster at 0 or below they cannot pay: the option GREYS OUT in battle (the in-battle grey-out is NOT the codex — the discovery table never greys anything, doctrine #2).

4. A7 — THE RESOLVER-STAGE CHECK. Affordability is evaluated against the THREADED hypothetical HP at the cast's slot in the plan (an earlier queued friendly splash on the caster counts). A cast that becomes unaffordable mid-pass resolves as SKIPPED-WITH-REASON — reuse the ResolvedOutcome.skipped machinery counters already use — and the queue previews the fizzle like any known outcome (Law #2). It is NOT a BREAK; it is fully predicted. Test both directions: affordable-at-queue-time/unaffordable-after-earlier-splash -> previewed fizzle; and the grey-out at menu time for flatly unaffordable casts.

5. LIMB-TAX TIEBREAK: prompt 7 left the aura limb tax breaking ties by enum order with a TODO(11). Now that affinity exists, ties break toward the PRIMARY affinity per the doc. Define primary deterministically (first/strongest affinity — follow alchemy-kit.md; if it is genuinely ambiguous, STOP and ask).

6. SCALING: aura scales transmutation damage as the weighted SUM over its elements (as coded); sum-vs-primary is a flagged-open tuning fork — keep the code path swappable behind one function, don't resolve the fork.

Do NOT touch: materia (parked grill — consumption/recharge/dowsing/strain-offset all deferred; leave the strain check's materia seam comment), naming/register content passes, mark-lexicon roster, carving UX polish, capacity/strain TUNING (playtest-gated). Doc silent -> STOP and ask. When done, comment on issue #30 with the 🤖 Claude says: provenance format (via --body-file, never inline non-ASCII) and flip its label per the workflow if a human step remains.

Done when: Rebecca-rule holds (no affinity = the option never appears), aura grows only within affinity, rune knobs validate at load, sub-floor casts cost strain that greys out at <= 0 and NEVER touches the lifecycle (test: a 1-HP caster simply cannot pay — no self-down exploit), a mid-pass-unaffordable cast previews and resolves as a skipped fizzle, the limb tax tiebreaks toward primary affinity, suite green, #30 updated, CLAUDE.md architecture map notes the doctrine layer, committed.
```
