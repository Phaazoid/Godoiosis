git checkout -b feature-name

git add .
git commit -m "message"
git push -u origin feature-name

# later

git checkout main
git merge feature-name
git push

git branch -d feature-name
git push origin --delete feature-name

Spend 30 minutes reading about EF Core.
Add a TicketService to your Blazor app.
Inject that service into the page.
Make the page use the service instead of its local list.
Learn enough LINQ to query tickets.
Read one article about Azure App Service and Azure SQL.
Prepare 3 stories:
difficult bug
learning something quickly
working with non-technical stakeholders