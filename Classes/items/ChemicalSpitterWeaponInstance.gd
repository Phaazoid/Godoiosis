class_name ChemicalSpitterWeaponInstance
extends WeaponInstance

# Pure pass-through — Chemical Spitter has no signature-mechanic state yet
# (Bio-hazard is undesigned). Exists so weapon_type dispatch (#82) has a real class to build.
