class_name ChainswordWeaponInstance
extends WeaponInstance

# Pure pass-through — Chainsword has no signature-mechanic state yet (Revved is
# undesigned). Exists so weapon_type dispatch (#82) has a real class to build.
